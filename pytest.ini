[pytest]
# Only collect the real unit tests under tests/. This deliberately excludes
# scripts/ (which holds live API probe scripts, not tests).
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
